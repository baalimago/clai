name: Simple Go Pipeline - validate

on:
  push:
    branches: ["main"]
  pull_request:
    branches: ["main"]

jobs:
  call-workflow:
    uses: baalimago/simple-go-pipeline/.github/workflows/validate.yml@slivingdoc
    with:
      go-version: "1.26"
      staticcheck-version: "latest"
      gofumpt-version: latest
