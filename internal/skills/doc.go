/*
Package skills implements discovery, parsing, trust, activation, and runtime policy handling for clai skills.

Skills are discovered from configured roots, resolved by source precedence, exposed to the model through lightweight descriptors, and loaded on demand through the internal load_skill flow.
*/
package skills
