package text

// ShellContext is a name of a shell context definition file under <configDir>/shellContexts/<name>.json.
// When non-empty, the rendered shell context will be appended to the final user prompt.
