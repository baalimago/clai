{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch ChatGPT - Glob",
      "type": "go",
      "request": "launch",
      "program": "${workspaceFolder}",
      "args": [
        "-cm",
        "gpt-4-turbo-preview",
        "glob",
        "*.go",
        "I have two files which are practically identical to this. How can I generalize the solution to reduce the amount of repeated code? The primary part I'm unsure of is how to set the apiKey"
      ],
      "env": {
        "NO_COLOR": "true"
      }
    },
    {
      "name": "Launch Claude - Glob",
      "type": "go",
      "request": "launch",
      "program": "${workspaceFolder}",
      "args": [
        "glob",
        "go.mod",
        "I have two files which are practically identical to this. How can I generalize the solution to reduce the amount of repeated code? The primary part I'm unsure of is how to set the apiKey"
      ],
      "env": {
        "NO_COLOR": "true"
      }
    }
  ]
}
